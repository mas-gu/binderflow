# Host environment (boltz conda env) requirements
numpy>=1.24
matplotlib>=3.7
gemmi>=0.6
plip>=2.3.0

# Web interface (optional — only needed when running the web server)
fastapi>=0.110
uvicorn[standard]>=0.29
jinja2>=3.1
python-multipart>=0.0.9
aiofiles>=23.0
aiosqlite>=0.20
pydantic>=2.0
scipy>=1.11
pandas>=2.0
